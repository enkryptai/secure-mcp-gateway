# Enkrypt AI Guardrails for LangGraph/LangChain Agents
# Python 3.9+ required

# Core dependencies
requests>=2.28.0
urllib3>=1.26.0

# LangGraph/LangChain (required)
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# Model providers (at least one required for full demos)
langchain-openai>=0.2.0
# langchain-anthropic>=0.2.0  # Optional: For Anthropic models
# langchain-aws>=0.2.0        # Optional: For AWS Bedrock models

# Development dependencies (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
