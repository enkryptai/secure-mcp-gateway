{
  "type": "monitor",
  "name": "MCPGW: Toxicity / NSFW Surge",
  "monitor_type": "bucket_level_monitor",
  "enabled": true,
  "schedule": {
    "period": { "interval": 1, "unit": "MINUTES" }
  },
  "inputs": [
    {
      "search": {
        "indices": ["ss4o_metrics-gateway*"],
        "query": {
          "size": 0,
          "query": {
            "bool": {
              "filter": [
                { "term":  { "name": "enkrypt.guardrail.blocks" } },
                { "terms": { "metric.attributes.violation_type": ["toxicity", "nsfw"] } },
                { "term":  { "aggregationTemporality": "AGGREGATION_TEMPORALITY_DELTA" } },
                { "range": { "time": { "from": "now-5m", "to": "now" } } }
              ]
            }
          },
          "aggregations": {
            "composite_agg": {
              "composite": {
                "size": 50,
                "sources": [
                  { "server": { "terms": { "field": "metric.attributes.enkrypt@server@name" } } },
                  { "tool":   { "terms": { "field": "metric.attributes.enkrypt@tool@name" } } }
                ]
              },
              "aggregations": { "sum_value": { "sum": { "field": "value" } } }
            }
          }
        }
      }
    }
  ],
  "triggers": [
    {
      "bucket_level_trigger": {
        "name": "surge",
        "severity": "3",
        "condition": {
          "buckets_path": { "sum_value": "sum_value" },
          "parent_bucket_path": "composite_agg",
          "script": { "source": "params.sum_value > 5", "lang": "painless" }
        },
        "actions": [
          {
            "name": "slack",
            "destination_id": "${SLACK_CHANNEL_ID}",
            "throttle_enabled": false,
            "subject_template": { "source": "MCP Gateway: Toxicity / NSFW Surge", "lang": "mustache" },
            "message_template": {
              "source": ":warning: *MCP Gateway -- Toxicity / NSFW Surge*\nMore than 5 toxicity or nsfw blocks observed against a single (server, tool) in the last 5 minutes.\n{{#ctx.alerts}}\n  Bucket: {{bucket_keys}}\n{{/ctx.alerts}}",
              "lang": "mustache"
            }
          }
        ]
      }
    }
  ]
}
